import ILyricsLine from '@src/types/interfaces/iLyricsLine';
import ISong from '@src/types/models/iSong';

export default function generateAndSavePdf(
  song: ISong,
  lyrics: ILyricsLine[],
  chordsVisible: boolean,
  fontMonospace: boolean,
  transShift: number
) {
  console.log('XD');
}
